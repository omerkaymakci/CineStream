server:
  port: 8082

spring:
  application:
    name: video-service

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: video-service-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.ByteArrayDeserializer


  datasource:
    url: jdbc:postgresql://localhost:5434/video_db
    username: video
    password: video
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true


cloudflare:
  r2:
    bucket: media-storage
    endpoint: https://a35e038b62ce4e86b844088c1c0ada53.r2.cloudflarestorage.com
    access-key: a59f6bf253ce8fa11681c7eaaf816edf
    secret-key: 122ef46274d9353ab999fb72b23b972810106b3ebb2d24af7ef5a5e50e69af09